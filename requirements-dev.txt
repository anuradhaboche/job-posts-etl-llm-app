# Full dependency set for local development and running the pipeline
# (LLM extraction, Airflow DAG, PDF parsing, tests).
# The deployed Streamlit dashboard uses the lean doc_extraction/requirements.txt.
anthropic>=0.25.0
pydantic>=2.0.0
duckdb>=1.3.1
snowflake-connector-python>=3.5.0
apache-airflow>=2.8.0
PyMuPDF>=1.23.0        # PDF text extraction
python-dotenv>=1.0.0
streamlit>=1.32.0
pandas>=2.0.0
plotly>=5.18.0
pytest>=8.0.0
